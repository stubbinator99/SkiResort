package com.skiresort.mappers;

import com.skiresort.models.Pass;
import com.skiresort.models.PassWithPrice;
import com.skiresort.models.Price;

public class PassMapper {


    /*public static PassWithPrice mapFromPassWithPrice(PassWithPrice pwp) {
        PassWithPrice pwp = new PassWithPrice();
        pwp.setPassId(pass.getPassId());
        pwp.setUsername(pass.getUsername());
        pwp.setStartDate(pass.getStartDate());
        pwp.setEndDate(pass.getEndDate());
        pwp.setPriceId(pass.getPriceId());
        pwp.setPassType(price.getPassType());
        pwp.setPassCategory(price.getPassCategory());

        return pwp;
    }*/
}
